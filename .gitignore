# Node.js dependencies
# These are installed via 'npm install' and should not be in version control
/node_modules
/app/node_modules

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Terraform files
# State files can contain sensitive data and are specific to your deployment.
# For collaboration, a remote backend (like S3) should be used instead of Git.
*.tfstate
*.tfstate.*
.terraform/
# Ignore .tfvars files as they often contain sensitive variables
*.tfvars
crash.log

# Operating System / IDE specific files
.DS_Store
Thumbs.db
.idea/
.vscode/