<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Our Products</title>
    <style> body { font-family: sans-serif; } .product { border: 1px solid #ccc; padding: 10px; margin-bottom: 10px; } </style>
</head>
<body>
    <h1>Welcome to Our Store</h1>
    <% products.forEach(function(product) { %>
        <div class="product">
            <h2><%= product.name %></h2>
            <p>Price: $<%= product.price %></p>
        </div>
    <% }); %>
</body>
</html>